apiVersion: v1
kind: Service
metadata:
  name: postgres-svc
  namespace: cad
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None   # Headless Service for StatefulSet DNS
