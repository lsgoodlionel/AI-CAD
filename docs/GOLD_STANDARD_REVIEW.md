# 金标准盘点与后续蓝图

> 数据截至 2026-09-08。全部数字来自 `apps/api/data/model3d/gold/` 的 **1096 条独立裁决**，
> 判据固定在 `CRITERIA.md`。本文只写实测得出的结论，推断处会标明「相关不等于因果」。

---

## 一、盘点：22 个类文件，1096 条裁决

| 类 | 单元 | 裁决 | 真阳率 | 判据回指 |
|---|---|---|---|---|
| `axis_grid_presence` | 1 | 80 | **92%** | — |
| `floor_assignment` | 2 | 80 | 76% | — |
| `has_discipline` | 2 | 90 | 74% | ✅ |
| `beams_retest` | 2 | 46 | 62%（主组） | ✅ |
| `walls` | 13 | 47 | 70%（主组） | ✅ |
| `walls_retest` | 2 | 49 | 57%（主组） | ✅ |
| `columns`（verdicts_v1） | 109 | 120 | 59% | — |
| `beams` | 15 | 50 | 56% | — |
| `building_unit` | 2 | 80 | 38% | — |
| `drawing_scale` | 4 | 56 | **30%** | ✅ |
| `column_outline` | 2 | 60 | 22% | ✅ |
| `equipment` | 20 | 60 | 17% | — |
| `columns_final` | 3 | 58 | **12%（kept 组）** | ✅ |
| `slabs` | 21 | 50 | 10% | — |
| `pipes_retest` | 2 | 52 | 15%（0% 有把握口径） | ✅ |
| `pipes` | 20 | 58 | **0%** | — |
| `rule_vs_model` | 3 | 60 | 分组见下 | ✅ |

**五个类没有精确率数字**（方法是 `count`/`text`/`fields`/`instances` 而非 `verdicts`）：
`axes`(2)、`columns_v1`(72)、`drawing_title`(185)、`drawing_usable`(36)、`title_block`(10)。
它们记的是计数或字段，不产出「对/错」比例。

**已知瑕疵的处理结果**（第四工作包）：

- `patch_verdicts_v1.json` **不是结构缺陷，是原始答卷** —— 它的 120 行与 `verdicts_v1.json`
  的 120 条裁决 ref 完全相同、`ok` 零处不一致。把它改成「标准结构」会让总数虚增到 1216。
  改法是让它自己声明身份：`kind: raw_judgement_sheet` + `materialized_in`。
- 22 个文件补上 `criteria` 字段：5 个回指 `CRITERIA.md` 的真实小节，
  9 个标 `UNFIXED: 理由`。**没有事后编一份判据填进去** —— 前 CRITERIA 时代的批次
  当时用的什么判据已不可考，编一个填上等于伪造可比性。覆盖率 13/22。
- 结构校验 `core/model3d/gold/schema_check.py`（error 0 / warn 1）。

**本文的三处数字曾经是错的**（`walls` 整行漏抄、`columns` 两极用了瓦片级、
误检三分分母残缺），根因是同一个：**数字靠手抄进文档**。
现在 `scripts/model3d/gold_report.py` 能一键重算这一节的每个数
（`--check-only` 可进 CI）。**改判据或加批次后应重跑它，而不是手改本文。**

---

## 二、五条核心发现

### 1. 误检按图纸聚集 —— 平均 **72% 的图纸是「全对」或「全错」**

只看每张图 ≥2 格的单元。**这里的「图」必须是真图纸**：`columns` 一批的单元是
**同一张图上切出的瓦片**，按单元聚合会把一张图算成十几张，两极占比虚高到 82%；
下表已按 `source.drawing_id` 归回图纸级。另有 11 个类每图仅一个单元，
图纸级聚合在它们身上无意义，不计入均值（报表里标 `unit(退化)`）。

| 类 | 图数 | 全对 | 全错 | 混合 | **两极占比** |
|---|---|---|---|---|---|
| pipes | 19 | 0 | 19 | 0 | **100%** |
| equipment | 20 | 2 | 15 | 3 | **85%** |
| columns | 20 | 4 | 3 | 13 | **35%** |
| beams | 14 | 6 | 5 | 3 | **79%** |
| walls | 13 | 7 | 2 | 4 | **69%** |
| slabs | 8 | 0 | 5 | 3 | **62%** |

**这是全部数据里最强的结构信号。** 它说明问题不在构件级判据的松紧，
而在**图纸级**：一张图要么整张能识别，要么整张不能。
继续调构件阈值，最多在「混合」那 28% 里挪动。

**`columns` 的 35% 是唯一的例外，而它恰好是最该注意的一个**：柱的误检真的散布在
图内（座椅、标高符号与柱混在同一张图上），图纸级闸对它帮不上忙。
杠杆在图纸级这个结论对其余五类成立，对柱不成立。

### 2. 误检构成三分，构件级判据只管得了其中一份

559 条误检归一后：

| | 占比 | 内容 |
|---|---|---|
| **别的真实构件** | **47%**（265）| 墙 87、柱 46、梁 42、座椅 28、房间轮廓 27 |
| **图面标注/非实体** | **25%**（142）| 空白/单线 58、标高符号 21、尺寸引出线 19、文字 14、填充 12 |
| **图纸级属性问题** | **24%**（133）| 本就无空间范围 43、比例错 39、无专业 23、楼层判错 19 |
| 未具名 + 判读侧答案 | 3%（19）| 判读者没说清，无法行动 |

24% 那一份**根本不该进构件识别**——图纸级闸就能挡掉。
25% 那一份是可写判据的具名对象。剩下 47% 才是真正的构件辨识难题。

（上一版写 44/24/22，合计只有 90% —— 那是归一表尚未覆盖全部写法时的残缺分母。
`taxonomy.py` 补全后 559 条全部落桶、`unmapped` 为空，三分的**结构没变**，
只是各自补回了本就属于它们的那几条。）

### 3. 比例：置信度**指向相反方向**（确证）；而 30% 这个数**已被推翻为不可直接引用**


    confidence = 1.00  →  合理率 24%
    confidence < 1.00  →  合理率 37%

根因在 `services/drawing_transform.py:154-159`：

```python
# confidence 要同时反映**轴号识别质量**与**比例尺是否标准**。
confidence = round(label_score * scale_score, 4)
```

**它衡量的是轴号识别质量 × 比例是否是常用值，与比例对错无关。**

梁批**独立同向**佐证：有「满分置信」变换记录的图，梁 44%；无记录的 78%（差 34 个百分点，n=16 vs 18）。
两个不同问题、不同样本，指向同一件事。**这一条站得住。**

**但「比例只有 30%」这个数不能再直接引用。** 比例批的接触表把 1872×1324 的裁图
缩到 **420 像素**，而判据要求拿门（1m）、楼梯踏步（0.3m）比长度 —— 在一张 1:150
的图上它们缩完只剩 **7 像素**和 **2 像素**，判读者要用的参照物根本看不见。

按原分辨率复核 5 张（同样的裁法与红线，只是不缩小，`scripts/model3d/verify_scale_bar.py`）：

| 图 | 系统比例 | 原分辨率实测 | 判读 | 谁对 |
|---|---|---|---|---|
| M4YE | 1:50 | 图上尺寸链 → 7.7 m | too_short | **判读错** |
| 9WH3 | 1:150 | 墙厚 → 7.5 m | too_short | **判读错** |
| Y4CX / REAV | 1:150 | 吻合 8 m | too_short | **判读错** |
| 4HNR | 1:350 | 踏步/门 → 2.4~4.4 m | too_short | **判读对** |

所以不是「金标准坏了」——**判读里有真信号，也有伪影，两者混在一起**。
（4HNR 的 1:350 本就不在 GB/T 50001 §6.0.4 表内，两条独立证据同向。）

更麻烦的是两者**共线**：红线占画面比例 = 8m ÷ 裁格覆盖米数，而覆盖米数正比于
scale 本身，所以「scale 大的图更容易错」与「scale 大时红线看着短」在这 56 条上
分不开（scale 单独预测「判读说不合理」AUC = **0.688**，强于任何一条比例证据）。

这正是 `CRITERIA.md` 里早已登记的那条「已知混淆」——**登记了不等于挡住了**。
重测前，30% 只能当「比例确有问题」的存在性证据。存疑标注已写进
`drawing_scale_v1.json` 每个 unit 的 `note`。

### 4. 已证实有效的东西 —— 两道闸 + 一套仪器

- **密排阵列闸**（座椅/吸声板）：删掉 22 格，**误删 0 格**
- **空白对照**：三批 12/12、12/12、10/10 全部答对 → 仪器可信
- **判读者重测信度 74%** —— 差异小于此不该当成不同

### 5. 结构性缺口 —— 有些事不是算法问题

- 全库 4109 张图，可用建筑剖面只有 **9 张（0.22%）** → 跨视图 z 恢复没有原料
- `section_json` 与 `model_component_sections` **全空** → 截面表从未生效，算量走默认值
- `grid_ref`（Phase H 声称的主键）只有 **42.58%** 有值
- 存量场景比当前代码多 **46%/82%** 的柱 → **所有修复都还没到达用户**

---

## 三、四个工作包的结果（并行开发，已全部合并）

1096 条裁决已经把问题定位清楚，四个包把结论落成了代码。**四个包里有三个
推翻了自己的任务简报**，下面按「达成了什么 / 没达成什么 / 推翻了什么」如实记。

### A · 图纸级准入闸 ✅ 达成

`services/drawing_gate.py`，输入一张图输出 `build / degrade / skip` + 原因 + 依据链，
判据互不短路（全部跑完才出结论）。

**推翻了简报的一条**：`unknown_role` 原本设计成降级。36 张样本上误伤 0%，
把样本扩到 **295 张**并**逐判据对齐各自真值维度**后，误伤 **75%** —— 而该维度
基线只有 37%，即它是**反信号**，已移除。测试把这条边界钉死了。

> 我自己在这里也犯过同一类错：第一次扩展回测把五批**不同维度**的真值混成一个
> 「有问题」，得出 non_geometric 误伤 56%。`drawing_scale` 的 not-ok 意思是
> 「比例不合理」，与「该不该建模」是两回事。**把不同问题的真值当同一个用，
> 与「判据不固定」是同一类错误。**

### B · 比例可信度重建 ⚠️ 硬指标未达成，但**查清了原因**

`core/model3d/scale_evidence.py`：图上印刷 1:N（0.70，唯一能独立确立可信度的
证据）+ §6.0.4 标准分母（0.10）+ 图幅覆盖（0.20），每条证据可解释。
旧口径改名 `label_confidence`（migration 051）——它衡量的是轴号识别质量。

`_blend_confidence` 立了一条规矩：**弱证据只能往下压，不能把分数抬上去**。
不加这条，无 OCR 注入时 1866/2142 会拿到 1.00，`scale_gate` 可信数从 1290
涨到 **1891，比旧公式更糟**；加了之后 1290 → 1290。

**验收硬指标「新分数与 56 条正相关」没有达到**（AUC 0.431 vs 旧 0.442）。
原因见 §二.3 —— **靶子本身混入了伪影**。要「通过」验收就得把「裁格覆盖 ≈ 10 米」
编进分数里，那是在编码仪器的分辨率缺陷，没有这么做是对的。

**量过但未采用的证据**（AUC ≈ 0.5，写进模块文档免得下一个人重走）：
墙厚窗口 0.42~0.47、线长分位 0.41~0.45、模数吸附 0.36~0.46、
轴距 6~9m（**循环论证** —— `_detect_scale` 读不到比例时本就是用 `8.4/轴距` 反推的）、
尺寸链只对上 1/23。

### C · 图面标注/非实体过滤器 ✅ 达成，且**推翻了简报的主要预期**

`core/model3d/annotation_filter.py`：`thin_stroke`（真实短边 <0.10m 且真实长宽比 ≥8）
+ `stroke_cluster`（真实角点 >24，只在调用方另传原始多边形时生效——构件字典里的
轮廓已被压到 8 点，**不传就不判，不假装判过**）。

两个阈值都回指识别器已有断言，不是新数。长宽比那条是**抵御比例错误的唯一防线**：
短边是米、会跟着错误比例缩放，长宽比不会。

实测 120 张图 4376 个柱候选：删 **13.0%**，独立判读 50 格（含空白对照 **10/10 全对**）
**误删 0**。

**推翻的预期**：简报说「标高符号是最大误检来源，本闸应该管它」。**打不中，而且
不该为打中去放宽阈值** —— `∨` 是整个符号作为一个多边形进来的，真实范围
**0.32×0.32m 近方形**，落在柱窗口正中，放宽到能删它就会同时删掉同尺寸的真柱。
三个独立口径同向（金标准复跑命中 0.0%、档案标高条目数 rho=+0.18）。已加用例钉死。

**两条走不通的判据也记下了**（此前被标注数据否掉过，这次查清了为什么）：
- 三角形顶点数**量错了对象** —— 填充三角形由三条线段画成，抽取器把每条线段
  两端点都收进轮廓，点数**恒为 6**，旧结论「三顶点占 0%」是编码方式的必然。
  去重共线后复量，12.6% 确是三角形，但它们集中在结构图上是**构件剖面填充**，
  三角形不是标注的签名 —— 这条路本来就走不通。
- 墨迹填充率**没有可用的面积** —— 18.6% 的候选鞋带面积恒为 0（见下）。

**⚠️ 尚未接线进 `element_recognizer.py`**，所以这 13% 还没有到达用户。

### D · 金标准整理 ✅ 达成，且**证伪了本文三个数字**

`schema_check.py`（error 0 / warn 1）+ `gold_report.py`（Markdown / --json / --check-only，
已进 CI）+ `taxonomy.py`（60+ 写法 → 28 标签 / 5 桶，**未登记的写法会喊**，不静默丢弃）。

**推翻的**：`patch_verdicts_v1.json` **不是结构缺陷，是原始答卷** —— 120 行与
`verdicts_v1.json` 的 120 条裁决 ref 完全相同、`ok` 零处不一致。按简报「统一成标准结构」
会让总数虚增到 1216。改法是让它自己声明身份（`kind: raw_judgement_sheet`）。

`criteria` 覆盖 13/22，其余 9 个标 `UNFIXED: 理由` —— **没有事后编一份判据填进去**。
前 CRITERIA 时代的批次当时用的什么判据已不可考，编一个填上等于伪造可比性。

### 计划外：QTO 面积在自交轮廓上恒为 0 🔧 已修

C 顺带撞见，我独立复核后规模比它报的更大。轮廓降点后带重复点、四角走成
**对角交叉序**，两个三角形面积等值反号，鞋带和**恰好抵消为 0**：

    A(75.969,16.585) → B(75.769,16.585) → C(75.969,16.385) → D(75.769,16.385)
           上边              对角线             下边              对角线回

存量 **10170 个柱里 3166 个（31.1%）**、**514 块板里 69 块（13.4%）** 踩在上面。

**影响的是谁要说准**：`raw_saving_est` 来自钢筋测算与人工填的 extra_saving，
**不经过 QTO**，提案金额没有直接算错。错的是 QTO 汇总本身 —— 它进创效提案的
description，也显示在模型页算量面板，**是三审审批人看的依据**。

修法是只在朴素公式失效时用去重 + 凸包兜底，其余一律不动（69% 的柱数字一个不变），
兜底值一律标 `estimated`。修完：柱混凝土 82319 → 88010 m³（+6.9%），
柱模板 126071 → 120079 m²（**−4.8%，这是修掉原先的高估**）。

---

## 四、下一步

### N1 · 把已经做好的接到用户面前（最高优先）

三件东西做完了但**没到达用户**：

1. **C 的过滤器未接线** —— 接进 `element_recognizer.py`，13% 才生效。
2. **B 的最强证据未接线** —— `services/drawing_info_extractor.py:268` 需要改成
   `transform_from_geometry(geom, printed_texts=<档案 OCR 文本>)`。
   不接这一行，印刷比例只在 ~9% 的图上可得（矢量文字）；接上就是 **~90%**（档案 OCR）。
   参数接口 B 已经留好。
3. **存量场景比当前代码多 46%/82% 的柱** —— 所有修复都还没到达用户，模型需要重建。

### N1-② 已完成：档案 OCR 接给比例证据 —— 并量出一个**客观口径**的比例准确率

`services/drawing_info_extractor.py` 把已经跑好的 OCR 文本（**带位置**）
传给 `transform_from_geometry(printed_texts=...)`。

**位置是关键，不是可选项**：图上到处是形如 `1:N` 的东西 —— 坡度 `i≤ 1:4`、
配筋 `板面T1:2Q@200`、电气回路号 `AL/M3/1/1-M1:4F`、能效比 `EER1: 4.12`。
不带位置直接数票，小分母（1:1~1:6）误命中占 **14.6%**；带上位置让
`TITLE_BLOCK_WEIGHT` 生效后降到 **5.4%**（`1:2` 从 233 张图降到 41 张）。
而 `printed_scale` 是权重 0.70 的最强证据 —— **喂错它比不喂更糟**。

**可得性实测**（全库 4109 张，用 `scale_evidence` 自己的解析逻辑，
只认 §6.0.4 表内分母）：能解析出合法比例分母的 **2858 张 = 69.6%**。
B 报的是 ~90%，实测 69.6%；仍是矢量文字（~9%）的七倍以上。

**顺带量出的、比接线本身更重要的数**：在 2142 张有变换记录的图上，
1823 张（85.1%）拿得到印刷比例证据，其中

    系统算的比例 == 图上印刷的比例        1251 / 1823 = **68.6%**

这是个**客观可核对**的口径 —— 不依赖判读者、不受接触表分辨率影响，
与「比例只有 30%」（判读口径，已知混着仪器伪影，见 §二.3）形成直接对照。
两个数测的不是同一件事：30% 问「红线画得对不对」，68.6% 问「比例值本身对不对」。
**N2 重测应当以这个客观口径为锚**，而不是再测一次判读。

剩下的 572 张（31.4%）系统值与印刷值不一致 —— 那是真正该修的那批，
接线后它们的 confidence 会被压下去（实测新分数：升 433 · 降 222 · 不变 1168）。

### N1-③ 重建前发现的拦路问题：梁配筋图上的柱会重复计数

抽样对比「当前代码 vs 存量场景」时，两张主梁配筋图的柱数从 0 涨到 218 和 320。

**先纠正一个测量错误**：第一版对比给所有图强制传 `view_type="plan"`，
得出「墙 +104.7%、梁 −100%」。改用 `classify_view_type` 逐图判定后，
这两个数变成 **+0.0%** —— 那是用错参数造出来的假象，不是回归。

**新增的柱不是误检**：来自图层 `S-北区-F3-COLS$0$0S-COLS-HATCH`
（`COLS` = 柱），主梁配筋图上确实画着柱截面 —— 柱是梁的支座。这是
`c67e512`「`填充`/`HATCH` 豁免保住构件填充截面」的预期结果。

**但它们不该进模型**，三个事实叠加：

| | |
|---|---|
| 同层同时选用两类图 | 歌剧院地下一层 23 张结构平面 + 14 张梁配筋，1 层 13 + 14 |
| 聚合没有跨图去重 | 去重只覆盖轴线与圆检测补柱 |
| 空间上判不出重合 | `placed_drawings=0`，没有世界坐标 |

修法与 `is_beam_drawing_effective` 同构（「图名对图种有否决权」的第三面）：
梁配筋图的主题是梁的配筋，图上的柱是参照物。**关掉的风险面实测为零** ——
全库没有任何一层只靠配筋图提供柱。判据要求「梁」与「配筋」同时出现，
只看「配筋」会把板配筋图也关掉。

修完抽样（14 张构件最多的图）：柱 1422 → **1337（−6.0%）**，
而加闸前是 +31.9%；梁 1185 原样保留，主产物没被误伤。

**教训**：重建前先量，否则重建就是把问题固化进模型 ——
这轮如果直接重建，会往模型里灌进几百根重复柱，而表面上看是「修复生效了」。

### ✅ 已修：图元配额按类分配（原「分块识别」调查的结论）

**结论是分块不该做**。实测同一张图：

    A 当前 2万·整图      9.0s   柱  3
    B 全量 40万·整图     9.7s   柱 42     ← 几乎免费
    C 全量 40万·4×4块  117.9s   柱 42     ← 慢 12 倍，无额外收益

病根比「丢了 95%」精确：**线把配额吃光了**（2 万配额里线占 98.8%，
多边形只剩 248 个），而柱与板来自多边形。改为按类配额后，
16 张随机图实测：

| | 旧配额 | 新配额 | |
|---|---|---|---|
| 多边形总量 | 16,340 | **175,445** | **10.7×** |
| columns | 72 | **1,027** | **+1326%** |
| beams | 413 | 1,112 | +169% |
| pipes | 1,787 | 3,612 | +102% |
| equipment | 344 | 971 | +182% |
| walls | 113 | 123 | +9% |
| slabs | 18 | **6** | **−67%** ← 连带回归，已开任务 |

板的下降已诊断：`pick_fallback_slab_polygons` 按面积降序挑，而多边形
变多后最大的那个是**图框**（实测面积 top10 = 10878, 10193, 26, 15, 9…，
一整层通常 2000~8000 m²），图框套住其余候选，只剩一块。
修它需要实测判据，已开独立任务。

**这些数字使金标准必须重测** —— 旧的精确率都是在「柱只有 3 根」
那个条件下测的。

### ⚠ 前提性发现：所有识别结果都建立在每张图 **5%** 的图元上

追查重建前后的构件数波动时挖到底层：`MAX_PRIMITIVES = 20_000` 截断了
**100%** 的参与建模图（抽查 120 张，120 张全部触到上限）。而真实图元数是：

| | |
|---|---|
| 真实图元数中位 | **377,686** |
| 当前上限 | 20,000 |
| **实际用到** | **5.5%** |
| 丢弃 | 94.5% |

多张图恰好停在 400,000（复测时设的新上限），说明真实值**还更高**。

**这改变了对本文全部数字的理解**：柱 12%、墙 57%、梁 62% —— 这些精确率
都是在每张图 5% 的数据上算出来的。精确率仍然有意义（被判读的那些框
确实是识别器产出的），但**召回率无从谈起** —— 95% 的图元根本没进过识别器。

而且截断位置依赖 PDF 遍历顺序，**同一张图两次重建可以给出不同的构件数**。
此前它还是静默的（标记侧 `> MAX_PRIMITIVES` 与收集侧 `>= MAX_PRIMITIVES`
比较符不一致，`truncated` 永远是 False），所以这种波动看起来像「修复生效了」。
标记已修（`d9cfc9b`），上限本身待定 —— 见下。

**为什么不能直接提高上限**：数据量 ×20，而 `_pair_up` 是 O(n×k)；
当前重建 582 张图需 23 分钟，直接放开会到小时量级。正在实测
「全量整图」与「全量分块」的耗时差，用数据决定方案。

### ✅ 金标准升级：六类批次，三条教训焊进生成器（2026-09-12）

旧的批次生成器是一次性脚本（`mk_col2.py` 等，随探针清理移除），每批都得记得
那几条教训。现在正式化为三件工具，教训写进代码而不是靠记忆：

| 工具 | 做什么 |
|---|---|
| `scripts/model3d/gold_batch.py` | 出接触表 + 判读命令 + manifest |
| `scripts/model3d/gold_ingest.py` | 回收：编号纠错 → 有效性检查 → 加权汇总 → 写 gold json |
| `scripts/model3d/gold_backfill_counts.py` | 给旧批次回填每格所在图纸的候选数 |

**焊进去的教训**：

- **原生分辨率裁图**（教训①）：先定裁框，再按「裁框点数 → 格子像素」反算 DPI，
  只渲染裁框那一块。比例批栽过的「缩到 420px、门只剩 7px」从构造上不再可能。
- **分层 + 语料加权**（教训②）：按「工程·专业」分层，跨层按语料候选量加权，并报覆盖率。
- **批内重测对**：约 10% 的格子换编号、换页重复出现 —— 每批自带信度，不再只靠 74% 那一个数。
- **判据程序抽取**：从 `CRITERIA.md` 按小节抽，不手抄；只有指路没有正文的小节
  拒绝出批；生成器发出去的每个 `what` 词都必须被 taxonomy 认得（测试锁住）。

**覆盖率：4 类 → 6 类**。板与设备第一次有了固定判据（CRITERIA v10），
**两类此前的数字（板 0.10、设备 0.17）判据未固定，与此后批次不可比**。

#### 层内加权：一次被量出来的纠正

col3 目检时一页 11 格里 3 格落在标题栏（logo 字母、手写日期），看上去像标题栏
假柱成灾。**先量**：60 张平面图、7821 个柱候选的位置密度 —— 右下角（占页面 4.5%）
只装着 **0.8%** 的候选，比均匀分布还低，横纵都是中间高、边缘低。标题栏假设不成立，
**没有做按位置的图框闸**。

真正的问题在抽样：每图至多取 2 格、**不论这张图有多少候选**。实测每格所在图纸的
候选数跨度极大：

| 批次 | 候选数 最小 | 中位 | 最大 |
|---|---|---|---|
| col3 | 3 | 94 | 1165 |
| wall3 | 2 | 99 | 1142 |
| beam3 | 21 | 168 | 662 |

等权时 3 个候选的图与 1165 个候选的图各占 2 格。回收端现按
「所在图纸候选数 ÷ 从该图取的格数」层内加权；有格缺数就整批退回等权并标出来。

#### 本轮批次

| 批次 | 页 | 被测 / 空白 / 重测对 | 有候选的层 | 候选数 |
|---|---|---|---|---|
| col3 柱 | 6 | 48 / 8 / 5 | 6 | 已回填 |
| wall3 墙 | 5 | 44 / 8 / 4 | 6 | 已回填 |
| beam3 梁 | 3 | 22 / 8 / 2 | 3 | 已回填 |
| pipe3 管 | 3 | 16 / 8 / 2 | 2 | 原生 |
| equip3 设备 | 3 | 16 / 8 / 2 | 2 | 原生 |
| slab3 板 | — | 生成中 | — | 原生 |

**设备候选只出自机电图**（歌剧院 mep 1137、第二工程 mep 1482，其余各层恰为 0）——
识别器按专业闸住了。所以第一版「50 格里 42 格误检是柱或墙」，是机电图下面
**建筑底图**里的柱和墙，不是在读建筑图。

pipe3、beam3 的被测格偏少（16、22），精确率区间约 ±20 个百分点量级：
够判断「管线是否仍接近 0」，不够量细小的改进。

#### 途中抓到的、会让数字失真的坑

- **每格都静默渲染失败**：算出的 DPI 是浮点，PyMuPDF `get_pixmap(dpi=…)` 只收整数，
  宽泛 `except` 吞掉 TypeError —— col3 第一次扫了 7520 个候选、出 0 格、退出码 0。
  改走缩放矩阵（`render_budget.render_clip`），0 格时大声失败。
- **实现说明漏进判据正文**：v9 初稿把代码路径写进 columns 判据，会原样发给判读者，
  且让正文不再与 col2 逐字一致（与 12% 可比的前提）。已移出。
- **超时被识别器吞掉**：单图超时被 `recognize` 自己的宽泛 except 吞成空结果，
  那张图会被当成「0 个候选」计进权重。改用标志位判定。
- **并行命令共享工作目录**：一个快照用相对路径打包、恰逢另一个命令 `cd`，
  打出来的快照**没有 `scripts/`**，排队中的板批次开跑就会崩；已就地修复。

**下一步**：送判读 → 回收（`gold_ingest`）得到当前代码下六类的加权精确率；
YOLO 训练等这批结果 —— 瓦片导出已修好（切块后柱 ≥24px），但标注仍出自规则识别器，
标注质量没量清之前训练只会把规则的错学进去。

### N2 · 比例批在原分辨率下重测

这是解开 B 那个死结的唯一办法。重测要控制住两件事：**接触表分辨率按候选尺度定**
（C 已经踩过一次：150dpi 下把 0.28×0.07m 的粗斜线看成实心方块，提分辨率重渲后改了
自己的判读结论），以及**裁格覆盖米数**不能与被测的 scale 共线。

### N3 · 柱是唯一图纸级闸帮不上忙的类

它的图纸级两极只有 35%（其余五类 62~100%），误检真的散布在图内。
柱三批 119 条误检归一后：图面标注 29%、别的真实构件 26%、其余 45% 未落桶。
C 的过滤器管得了细笔画那一档，管不了标高符号（0.32×0.32m 近方，与真柱同尺寸）——
**标高符号要另找判据，不能靠尺寸窗口**。

### N4 · 结构性缺口（不是算法问题）

- 全库 4109 张图，可用建筑剖面只有 **9 张（0.22%）** → 跨视图 z 恢复没有原料
- `section_json` 与 `model_component_sections` **全空** → 截面表从未生效
- `grid_ref`（Phase H 声称的主键）只有 **42.58%** 有值

---
