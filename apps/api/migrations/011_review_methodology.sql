-- ============================================================
-- Migration 011: 会审审查引擎 V4 升级（方法论：控制链 + 五维审查 + 结构化处理建议）
-- ============================================================
-- 知识来源：~/work/031 图纸会审/06 方法论与AI原则/（4123 份工程会议纪要蒸馏）。
-- 依赖：必须先执行 007_review_audit.sql、008_review_audit_v2.sql、009_review_sop.sql。
-- 可重复执行（IF NOT EXISTS）；向后兼容（新增列可空，旧报告该列为 NULL）。
--
-- review_method 结构：
--   {控制链:{触发,边界,风险,责任,动作,闭环,闭环判定:{status,缺失项,追问项}},
--    五维审查:[{维度,状态,依据,追问}],
--    处理建议:[{动作,动作类型,责任方,配合方,输出件}],
--    闭环要求:{是否影响开工,是否影响穿插,是否需要专题会,下次复核节点},
--    优先对象:[{name,weight,hit}]}
-- ============================================================

ALTER TABLE ai_review_issues ADD COLUMN IF NOT EXISTS review_method JSONB;
