# Web 框架
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# 数据库
databases[asyncpg]==0.9.0
asyncpg==0.29.0

# 设置管理
pydantic-settings==2.4.0

# 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt>=4.0.1,<5
PyJWT==2.9.0

# Redis
redis[asyncio]==5.0.8

# HTTP 客户端（Ollama / 自定义 HTTP Provider）
httpx==0.27.2

# LLM SDK
anthropic==0.35.0
openai==1.51.0

# 模板渲染（custom_http provider）
jinja2==3.1.4

# JSONPath（custom_http 响应提取）
jsonpath-ng==1.7.0

# 对象存储
minio==7.2.9

# 工作流状态机
transitions==0.9.2

# 异步任务
celery[redis]==5.4.0

# 工具
python-dotenv==1.0.1

# ── Phase 2：AI 审图引擎 ─────────────────────────────────────

# 规则引擎 — YAML DSL
pyyaml==6.0.2

# 知识图谱引擎 — AGE 直连
# asyncpg 已包含（见上方）

# 视觉/OCR 引擎 — PDF + DXF 解析
pymupdf==1.24.10         # PDF 文本提取 (import fitz)
openpyxl==3.1.5          # Excel 报告生成
ezdxf==1.3.4             # DWG/DXF 解析

# RAG 引擎 — 向量检索
chromadb==0.5.12

# ── 测试依赖 ────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.2             # AsyncClient（已在上方，此处注释说明测试也需要）

# ── LangGraph 多轮推理（graceful degradation，可按需安装）──
# langgraph==0.2.28
# langchain-core==0.3.18

# ── 3D 模型基座：IFC → glb 几何层（Phase 6；缺失时模型仅楼层骨架）──
ifcopenshell==0.8.5   # 0.8.4 起才提供 linux/aarch64 wheel

# ── 图元检测（YOLOv8，vision_engine/yolo_detector 消费）──
ultralytics==8.3.202
Pillow==10.4.0

# ── 可选：重量级 OCR（按需安装，graceful degradation）──
# paddlepaddle==2.6.1
# paddleocr==2.8.1
