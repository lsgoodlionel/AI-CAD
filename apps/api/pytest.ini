[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --cov=core
    --cov=routers
    --cov=services
    --cov=tasks
    --cov-report=term-missing
    --cov-fail-under=80
